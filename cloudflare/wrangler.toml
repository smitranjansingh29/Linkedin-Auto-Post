name = "linkedin-auto-poster-webhook"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put <NAME>`, do not put values here):
#   GH_PAT
#   TELEGRAM_BOT_TOKEN
